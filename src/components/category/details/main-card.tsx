'use client';
import { Primitives } from '@/lib/ddd/types/primitives';
import { Category } from '@/modules/category/domain/category';

const MainCard = ({ category }: { category: Primitives<Category> }) => {
  return <></>;
};

export default MainCard;
